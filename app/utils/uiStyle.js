import { StyleSheet } from "react-native";

export const uiStyle = StyleSheet.create({
    input: {
      borderWidth: 1,
      borderColor: "#ccc",
      padding: 10,
      marginBottom: 10,
      borderRadius: 5,
    },
    container: {
      padding: 20,
      margin: 20,
    },
});